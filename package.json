{
  "name": "ai-flashcard",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop-first local knowledge flashcard tool with optional Obsidian export",
  "main": "dist-electron/electron/main.js",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"npm:dev:renderer\" \"npm:dev:electron:watch\" \"npm:dev:electron\"",
    "dev:renderer": "vite",
    "dev:electron:watch": "tsc -w -p tsconfig.electron.json",
    "dev:electron": "wait-on http://localhost:5173 dist-electron/electron/main.js && electron .",
    "build": "npm run build:electron && npm run build:renderer",
    "build:electron": "tsc -p tsconfig.electron.json",
    "build:renderer": "vite build",
    "typecheck": "tsc --noEmit -p tsconfig.electron.json && tsc --noEmit -p tsconfig.json",
    "validate:workflow": "npm run build:electron && node scripts/validate-workflow.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.8.1",
    "concurrently": "^9.2.1",
    "electron": "^41.2.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.8",
    "wait-on": "^9.0.1"
  }
}
